import { io } from 'socket.io-client'; export const createSocket = () => { const serverUrl = window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1' ? 'http://4.178.188.9:4000' : window.location.origin; console.log('Connecting to server at:', serverUrl); return io(serverUrl, { reconnectionAttempts: 5, reconnectionDelay: 1000, transports: ['websocket', 'polling'], timeout: 10000 }); };
